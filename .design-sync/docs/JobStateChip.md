---
category: Verdicts
---

Job process outcome: `PENDING`, `RUNNING` (pulses), `SUCCEEDED`, `FAILED`, `CANCELLED`, `INCONCLUSIVE` (the check could not run — quiet, not red). Never the artifact palette.
