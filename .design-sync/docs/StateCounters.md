---
category: Verdicts
---

The catalog header: leads with the UNOBSERVED count as the open question, then VERIFIED with its by-level split, then FAILED; optional caption, hint and fact pills. Counts come from the server over the whole table.
